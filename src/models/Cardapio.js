export class Cardapio {
    constructor(id, nome, descricao, tipo) {
      this.id = id; // ID único
      this.nome = nome; // Nome do item
      this.descricao = descricao; // Descrição do item
      this.tipo = tipo; // Categoria (Proteína, Acompanhamento, Salada)
    }
  }
  